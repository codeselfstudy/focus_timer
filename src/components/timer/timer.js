import React from "react";

import "./timer.scss";

const Timer = () => {
    return (
        <div className="timer">
            <div className="time">25:00</div>
        </div>
    );
};

export default Timer;
