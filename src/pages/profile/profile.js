import React from "react";

import "./profile.scss";

const ProfilePage = () => (
    <div className="profile">
        <h1>User Profile</h1>

        <p>
            This might be a page where users can do stuff with their profile. If
            users will be able to view other profiles, then that should be taken
            into consideration.
        </p>
    </div>
);

export default ProfilePage;
